# Local-only development values. Copy to .env and replace before shared use.
POSTGRES_DB=kube_foundry
POSTGRES_USER=kube_foundry
POSTGRES_PASSWORD=replace-with-a-local-password
REDIS_PASSWORD=replace-with-a-local-password

