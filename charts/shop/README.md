# Shop Helm chart

Planned for phase 5. The chart will package web, API, worker, PostgreSQL, Redis, Services, policies, and operational resources. Environment-specific replica counts, resources, hostnames, and image tags remain in `gitops/apps/shop/overlays`.

