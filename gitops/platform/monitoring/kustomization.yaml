apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
configMapGenerator:
  - name: shop-dashboard
    files:
      - shop.json
generatorOptions:
  disableNameSuffixHash: true
  labels:
    grafana_dashboard: "1"
