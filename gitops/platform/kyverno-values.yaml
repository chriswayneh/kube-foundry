# Single replicas keep the local development footprint small.
admissionController:
  replicas: 1
reportsController:
  replicas: 1
backgroundController:
  enabled: false
cleanupController:
  enabled: false
features:
  generateValidatingAdmissionPolicy:
    enabled: false
