# GitOps

Reserved for phase 8. Argo CD will use `root-app.yaml` as an app-of-apps entry point after the platform and shop environment manifests exist. No incomplete Application is committed during phases 1-3.

