# Development overlay

One API/web replica, 50m CPU and 64Mi memory requested by the API, and `shop.localhost`. Deploy with `make deploy-phase5 ENVIRONMENT=dev`. This is the default local profile. It targets `shop` in its own cluster.
