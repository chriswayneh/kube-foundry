# Shop base

`shop.yaml` is generated from `charts/shop` with `make render-shop`. Commit generated changes alongside chart edits; do not edit this file manually. `make check-chart` rejects a stale render.

The base adds the `shop` Namespace. Each overlay consumes this base and changes only environment-specific fields. GatewayClass and controllers are owned by the platform layer.
